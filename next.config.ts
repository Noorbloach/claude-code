import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Standard Next.js server-side build config.
  // This enables Serverless API Route proxies in production (Vercel) and local development.
};

export default nextConfig;
